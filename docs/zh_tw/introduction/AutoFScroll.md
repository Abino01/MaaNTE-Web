# 粉爪快速取用

## 簡介

粉爪快速拾取功能，開啟後將一直按住 F 並滾動滑鼠滾輪實現極速拾取。

需要先按住 F 觸發啟動。

## 功能

啟動後自動模擬 F 鍵按壓和滑鼠滾輪滾動，實現快速拾取地面物品。

此任務無可配置選項。

## 配置詳解

此任務無子選項，無需額外配置。

**具體實作**：任務 `AutoFScroll`，無 `option` 欄位。透過自訂動作 `auto_f_scroll` 實現持續 F 鍵按壓和滾輪滾動。